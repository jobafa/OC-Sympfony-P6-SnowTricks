<form method="post" action="{{ path('app_annonces_delete', {'id': annonce.id}) }}" onsubmit="return confirm('Êtes vous sûr(e) de vouloir supprimer cette image ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.id) }}">
    <button class="btn">Supprimer</button>
</form>