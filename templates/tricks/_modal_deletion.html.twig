<div class="modal fade" id="trickDeleteModal" tabindex="-1" role="dialog" aria-labelledby="trickDeleteModal" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="trickDeleteModalTitle">Suppression de Figure</h5>
				{# <button type="button" class="close" data-dismiss="modal" aria-label="Close">
					<span aria-hidden="true">&times;</span>
				</button> #}
			</div>
			<div class="modal-body">
				Voulez vous supprimer cette figure ?
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-info" data-dismiss="modal">Annuler</button>
				<form action="{{ path('trick_delete', {'id': trick.id})}}" method="post" id="trick_delete">
					<input type="hidden" name="_method" value="DELETE">
					<input type="hidden" name="_token" id="delete_trick_" value="{{ csrf_token('delete_trick_' ~ trick.id) }}">
					<button class="btn btn-danger ml-2">Confirmer</button>
				</form>
			</div>
		</div>
	</div>
</div>